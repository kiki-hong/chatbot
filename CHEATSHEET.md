# 챗봇 프로젝트 명령어 모음 (Cheatsheet)

이 파일은 프로젝트 관리에 필요한 주요 명령어들을 모아둔 것입니다. 터미널(PowerShell 또는 CMD)에서 실행하세요.

## 1. 🚀 서버 실행
웹사이트를 내 컴퓨터에서 실행하고 확인합니다.
```bash
npm run dev
```
- 접속 주소: `http://localhost:3000`
- 종료 방법: 터미널에서 `Ctrl + C`

## 2. 📚 데이터 관리 (PDF 변환 & 학습)

### 2-1. 모든 챗봇의 PDF 변환
`data/[botId]/raw/` 폴더에 있는 모든 PDF 파일을 변환합니다.
```bash
node scripts/convert-pdf.js
```

### 2-2. 특정 챗봇만 PDF 변환
특정 챗봇(예: `oriental`)의 파일만 변환하고 싶을 때 사용합니다.
```bash
node scripts/convert-pdf.js oriental
```
*(뒤에 `jisan` 등 다른 ID를 넣으면 해당 챗봇만 변환됩니다)*

### 2-3. 지식 목록(인덱스) 갱신
변환된 파일들을 챗봇이 인식할 수 있도록 목록을 업데이트합니다. **파일 변환 후 반드시 실행해주세요.**
```bash
node scripts/update-knowledge-index.js
```

## 3. 📦 배포 준비 (빌드)
서버에 올리기 전에 오류가 없는지 확인하거나, 배포용 파일을 만들 때 사용합니다.
```bash
npm run build
```

## 4. 🛠️ 기타 유용한 명령어

### 패키지 설치
`package.json`에 있는 라이브러리들을 설치합니다. (처음 시작할 때나 라이브러리 추가 후)
```bash
npm install
```
